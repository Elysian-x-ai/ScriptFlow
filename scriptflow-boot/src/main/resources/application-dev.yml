# Local development profile
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/scriptflow?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 12345

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

logging:
  level:
    com.scriptflow: DEBUG
    org.springframework: INFO
